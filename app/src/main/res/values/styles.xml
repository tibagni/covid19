<resources>
    <!-- Base application theme. -->
    <style name="AppTheme.v21" parent="Theme.MaterialComponents.Light">
        <item name="colorPrimary">@color/primaryColor</item>

        <!-- Action bar -->
        <item name="actionBarStyle">@style/AppActionBarStyle</item>
        <item name="actionBarBackground">@color/primaryLightColor</item>

        <item name="dividerColor">@color/dividerColor</item>

        <item name="android:statusBarColor">?attr/actionBarBackground</item>
        <item name="defaultBackground">@color/background</item>
    </style>

    <style name="AppTheme" parent="AppTheme.v21" />

    <style name="AppActionBarStyle" parent="Widget.MaterialComponents.Light.ActionBar.Solid">
        <item name="background">?attr/actionBarBackground</item>
        <item name="backgroundStacked">?attr/actionBarBackground</item>
        <item name="backgroundSplit">?attr/actionBarBackground</item>
    </style>

</resources>